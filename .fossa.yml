# Generated by FOSSA CLI (https://github.com/fossas/fossa-cli)
# Visit https://fossa.com to learn more
#
# Modified within nats-io
# nb: this repo is a library with no main, thus the modules[0].name field
# needs to be the full path, not just the final component.

version: 2
cli:
  server: https://app.fossa.com
  fetcher: git
  project: github.com/nats-io/jwt
analyze:
  modules:
    - name: github.com/nats-io/jwt
      type: go
      target: github.com/nats-io/jwt
      path: .
